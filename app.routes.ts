import { Routes } from '@angular/router';
import { TodoComponent } from './todo/todo';

export const routes: Routes = [
  { path: '', component: TodoComponent }, // Ana sayfa görev listesi


];
